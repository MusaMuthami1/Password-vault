{
  "name": "ciphervault-backend",
  "version": "1.0.0",
  "description": "CipherVault Password Manager Backend",
  "main": "server/index.js",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd server && npm install",
    "dev": "cd server && npm run dev"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": "18.x"
  }
}
